# How many total characters are there?
    # There are len(Characters.objects.all()) total Characters equal to 302
    # If we are to count characters and all character subtypes than their are 615 <len(Fighter.objects.all()) + len(Fighter.objects.all()) + len(Mage.objects.all()) + len(Cleric.objects.all()) + len(Thief.objects.all()) + len(Necromancer.objects.all())>
